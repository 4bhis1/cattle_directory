import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: 'export',
  images: {
    unoptimized: true,
  },
  // basePath: '/cattle_directory',
  // assetPrefix: '/cattle_directory',
};

export default nextConfig;
